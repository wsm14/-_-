import React, { useMemo, useState } from "react";
import { View, Text, Image } from "@tarojs/components";
export default (props) => {
  const { data } = props;
  return <View className="community_goodCard_box"></View>;
};
