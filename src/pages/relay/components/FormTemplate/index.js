import React, { useMemo, useState } from "react";
import { View, Text, Image } from "@tarojs/components";
import "index.scss";
export default (props) => {
  const { data } = props;
  
  return (<View className="FormTemplate_box"></View>);
};
